{
  "version": 2,
  "installCommand": "npm ci --legacy-peer-deps --no-audit --no-fund",
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["public/**"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.js" }
  ]
}
